<!-- Miao Zhang , S1402087 -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/backgroud_main"
    >

    <Button
        android:id="@+id/currentButton"
        android:text="Current Incidents"
        android:textColor="#FFFFFF"
        android:textSize="15sp"
        android:layout_width="200dp"
        android:layout_height="60dp"
        android:background="@drawable/button_customise1"
        android:shadowColor="#0f2140"
        android:shadowDx="0"
        android:shadowDy="0"
        android:shadowRadius="5"
        android:layout_above="@+id/plannedButton"
        android:layout_alignStart="@+id/plannedButton"
        android:layout_marginBottom="29dp" />

    <Button
        android:id="@+id/plannedButton"
        android:text="Planned RoadWork"
        android:textColor="#FFFFFF"
        android:textSize="15sp"
        android:layout_width="200dp"
        android:layout_height="60dp"
        android:background="@drawable/button_customise1"
        android:shadowColor="#0f2140"
        android:shadowDx="0"
        android:shadowDy="0"
        android:shadowRadius="5"
        android:layout_marginBottom="26dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <TextView
        android:id="@+id/textView2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="23dp"
        android:shadowColor="@color/colorPrimary"
        android:textColor="#FFFFFF"
        android:textSize="30sp"
        android:text="Mobile Platform Course Work"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <TextView
        android:id="@+id/textView3"
        android:textColor="#FFFFFF"
        android:textSize="30sp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Traffic ScotLand"
        android:layout_below="@+id/textView2"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="32dp" />

</RelativeLayout>